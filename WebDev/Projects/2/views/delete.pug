extends layouts/layout

block content
  h1 Delete Contact
  p Are you sure you want to delete this contact?
  form(action=`/:${contact.id}/delete`, method='post')
    button(type='submit') Yes, delete this contact
    a(href=`/${contact.id}`) No, go back